{
  "name": "livemeet-poc",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun run --filter @livemeet/api dev",
    "dev:web": "bun run --filter @livemeet/web dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
